# 🔄 升級邏輯遷移指南

## 📋 遷移概述

從 **每10題升級** 系統遷移到 **每3題升級** 系統，確保現有用戶數據的平滑過渡。

## 🎯 遷移目標

1. **統一升級邏輯**：所有用戶使用相同的3題升級規則
2. **保護用戶進度**：現有用戶不會失去已獲得的等級
3. **公平性原則**：新舊用戶享受相同的升級體驗

## 📊 數據庫變更

### 新增欄位
- `correct_in_level` (INTEGER): 當前等級答對題數，預設值為 0

### 現有欄位保持
- `user_id`: 用戶ID
- `correct`: 總答對題數
- `wrong`: 總答錯題數  
- `level`: 當前等級
- `last_update`: 最後更新時間

## 🔧 遷移策略

### 方案一：保守遷移（推薦）
```sql
-- 為所有現有用戶添加 correct_in_level 欄位，初始值為 0
UPDATE user_stats 
SET correct_in_level = 0 
WHERE correct_in_level IS NULL;

-- 現有用戶從下一題開始使用新的3題升級邏輯
-- 優點：簡單安全，不影響現有等級
-- 缺點：現有用戶需要重新累積當前等級的進度
```

### 方案二：智能遷移
```sql
-- 根據現有答對題數估算當前等級進度
-- 假設用戶在當前等級已經有一定進度
UPDATE user_stats 
SET correct_in_level = CASE 
    WHEN correct % 10 <= 3 THEN correct % 10
    ELSE 0
END
WHERE correct_in_level IS NULL;

-- 優點：給予現有用戶部分進度獎勵
-- 缺點：邏輯較複雜，需要仔細測試
```

## ⚡ 實施步驟

### 1. 數據庫準備
```sql
-- 檢查是否需要添加新欄位
ALTER TABLE user_stats ADD COLUMN IF NOT EXISTS correct_in_level INTEGER DEFAULT 0;

-- 為現有用戶初始化欄位
UPDATE user_stats SET correct_in_level = 0 WHERE correct_in_level IS NULL;
```

### 2. 代碼部署
- ✅ 已更新 `app_supabase.py` 中的升級邏輯
- ✅ 已添加進度反饋功能
- ✅ 已更新幫助訊息

### 3. 測試驗證
- ✅ 通過所有升級邏輯測試
- ✅ 驗證新用戶體驗
- ✅ 確認現有用戶兼容性

## 📈 遷移後的用戶體驗

### 新用戶
- 從等級1開始，答對3題升級
- 清晰的進度提示 (X/3)
- 更快的升級體驗

### 現有用戶
- 保持當前等級不變
- 從下一題開始使用新規則
- 獲得更好的進度反饋

## 🔍 監控指標

### 遷移成功指標
- [ ] 所有用戶都有 `correct_in_level` 欄位
- [ ] 新升級邏輯正常運作
- [ ] 用戶升級頻率提升
- [ ] 無用戶投訴或錯誤報告

### 性能指標
- 升級頻率：預期提升 3-4 倍
- 用戶參與度：預期提升
- 系統穩定性：保持不變

## 🚨 回滾計劃

如果遇到問題，可以快速回滾：

```sql
-- 回滾到舊的升級邏輯（僅限緊急情況）
-- 1. 恢復舊的升級函數
-- 2. 暫時忽略 correct_in_level 欄位
```

## ✅ 遷移檢查清單

### 部署前
- [x] 代碼修改完成並測試通過
- [x] 數據庫遷移腳本準備就緒
- [x] 回滾計劃制定完成
- [ ] 備份生產數據庫

### 部署中
- [ ] 執行數據庫遷移
- [ ] 部署新代碼
- [ ] 驗證基本功能

### 部署後
- [ ] 監控系統日誌
- [ ] 檢查用戶升級是否正常
- [ ] 收集用戶反饋
- [ ] 性能指標監控

## 🎉 預期效果

### 用戶體驗改善
- **升級頻率**：從每10題升級改為每3題升級
- **成就感**：更頻繁的升級慶祝
- **進度透明**：清晰的進度顯示 (X/3)
- **參與度**：預期提升用戶活躍度

### 系統優化
- **邏輯統一**：所有版本使用相同升級邏輯
- **代碼簡化**：移除重複的升級實現
- **維護性**：更易於維護和擴展

## 📞 聯絡支援

如果遷移過程中遇到任何問題，請立即：
1. 檢查系統日誌
2. 驗證數據庫狀態
3. 考慮是否需要回滾
4. 記錄問題詳情以便後續改進

---

**遷移負責人**: AI Assistant  
**遷移時間**: 2025年9月  
**預期完成**: 即時生效  
**風險等級**: 低（已充分測試）
