# 🔧 環境變量設置指南

## 📋 概述

本指南將幫助您設置解剖問答機器人所需的所有環境變量，確保排行榜功能和其他功能正常運作。

## 🎯 必需的環境變量

### 1. Supabase 配置 ✅ (已設置)
```bash
export SUPABASE_URL="https://ciqlfqfgzqqgdrogedxg.supabase.co"
export SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNpcWxmcWZnenFxZ2Ryb2dlZHhnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDcwODUsImV4cCI6MjA2Njc4MzA4NX0.LP-9iTyckifGXvS45GBWBImnBGKADAw0jk1BpGNZWWA"
```

### 2. LINE Bot 配置 ⚠️ (需要設置)
```bash
export LINE_CHANNEL_ACCESS_TOKEN="your_line_channel_access_token_here"
export LINE_CHANNEL_SECRET="your_line_channel_secret_here"
```

### 3. Facebook Messenger 配置 (可選)
```bash
export PAGE_ACCESS_TOKEN="your_page_access_token_here"
export VERIFY_TOKEN="your_verify_token_here"
```

## 🚀 快速設置方法

### 方法 1: 使用設置腳本
```bash
# 運行設置腳本
chmod +x setup_env.sh
./setup_env.sh
```

### 方法 2: 手動設置
```bash
# 設置 LINE Bot 環境變量
export LINE_CHANNEL_ACCESS_TOKEN="your_actual_token_here"
export LINE_CHANNEL_SECRET="your_actual_secret_here"

# 設置 Facebook Messenger 環境變量 (可選)
export PAGE_ACCESS_TOKEN="your_actual_page_token_here"
export VERIFY_TOKEN="your_actual_verify_token_here"
```

### 方法 3: 永久設置 (推薦)
將以下內容添加到您的 shell 配置文件 (`~/.bashrc` 或 `~/.zshrc`):

```bash
# 解剖問答機器人環境變量
export SUPABASE_URL="https://ciqlfqfgzqqgdrogedxg.supabase.co"
export SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNpcWxmcWZnenFxZ2Ryb2dlZHhnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDcwODUsImV4cCI6MjA2Njc4MzA4NX0.LP-9iTyckifGXvS45GBWBImnBGKADAw0jk1BpGNZWWA"
export LINE_CHANNEL_ACCESS_TOKEN="your_actual_token_here"
export LINE_CHANNEL_SECRET="your_actual_secret_here"
export PAGE_ACCESS_TOKEN="your_actual_page_token_here"
export VERIFY_TOKEN="your_actual_verify_token_here"
```

然後重新載入配置：
```bash
source ~/.bashrc  # 或 source ~/.zshrc
```

## 🔍 如何獲取 LINE Bot 憑證

### 1. 前往 LINE Developers Console
- 訪問: https://developers.line.biz/console/
- 登入您的 LINE 帳號

### 2. 創建或選擇 Provider
- 如果沒有 Provider，請創建一個
- 選擇現有的 Provider

### 3. 創建或選擇 Channel
- 選擇 "Messaging API" 類型
- 填寫必要的 Channel 資訊

### 4. 獲取憑證
- 在 Channel 設定頁面找到：
  - **Channel access token** → 這就是 `LINE_CHANNEL_ACCESS_TOKEN`
  - **Channel secret** → 這就是 `LINE_CHANNEL_SECRET`

### 5. 設置 Webhook URL
- 在 Messaging API 設定中設置 Webhook URL
- 例如: `https://your-domain.com/webhook`

## 🔍 如何獲取 Facebook Messenger 憑證

### 1. 前往 Facebook Developers Console
- 訪問: https://developers.facebook.com/
- 登入您的 Facebook 帳號

### 2. 創建應用程式
- 選擇 "Business" 類型
- 填寫應用程式資訊

### 3. 添加 Messenger 產品
- 在產品列表中添加 "Messenger"
- 設置 Webhook URL 和驗證令牌

### 4. 獲取憑證
- **Page Access Token** → 這就是 `PAGE_ACCESS_TOKEN`
- **Verify Token** → 這就是 `VERIFY_TOKEN`

## ✅ 驗證設置

### 檢查環境變量
```bash
# 檢查所有環境變量
env | grep -E "(LINE|SUPABASE|PAGE)"

# 或使用我們的檢查腳本
python -c "
import os
print('SUPABASE_URL:', '✅' if os.getenv('SUPABASE_URL') else '❌')
print('SUPABASE_ANON_KEY:', '✅' if os.getenv('SUPABASE_ANON_KEY') else '❌')
print('LINE_CHANNEL_ACCESS_TOKEN:', '✅' if os.getenv('LINE_CHANNEL_ACCESS_TOKEN') else '❌')
print('LINE_CHANNEL_SECRET:', '✅' if os.getenv('LINE_CHANNEL_SECRET') else '❌')
print('PAGE_ACCESS_TOKEN:', '✅' if os.getenv('PAGE_ACCESS_TOKEN') else '❌')
"
```

### 測試排行榜功能
```bash
# 運行測試腳本
python test_leaderboard_flex.py
```

## 🚨 常見問題

### Q: 為什麼排行榜沒有出現 Flex 訊息？
A: 請檢查 `LINE_CHANNEL_ACCESS_TOKEN` 是否正確設置且有效。

### Q: 如何確認環境變量是否生效？
A: 運行 `./setup_env.sh` 腳本或使用上述驗證命令。

### Q: 環境變量設置後需要重啟應用嗎？
A: 是的，設置環境變量後需要重啟 Flask 應用才能生效。

### Q: 可以只設置 LINE Bot 而不設置 Facebook Messenger 嗎？
A: 可以，Facebook Messenger 配置是可選的。

## 📞 需要幫助？

如果遇到問題，請檢查：
1. 環境變量是否正確設置
2. LINE Bot 憑證是否有效
3. 應用是否已重啟
4. 日誌中是否有錯誤訊息

---

**注意**: 請勿將真實的憑證直接提交到版本控制系統中。建議使用環境變量或安全的配置管理方式。
