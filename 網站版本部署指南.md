# 解剖學測驗系統 - 網站版本部署指南

## 概述

這個網站版本讓學生可以透過LINE登入，並與現有的LINE機器人共享相同的遊戲數據。學生無論在LINE上還是網站上玩，都會保持相同的遊戲記錄。

## 功能特色

### 🔐 LINE登入整合
- 使用LINE Login進行身份驗證
- 與現有LINE機器人共享用戶數據
- 自動同步遊戲記錄

### 🎮 完整遊戲體驗
- 14個等級系統（新手解剖師→終極解剖師）
- 即時排行榜更新
- 答題後立即顯示正確答案和解釋
- 響應式設計，支援手機和電腦

### 📊 數據同步
- 使用相同的Supabase數據庫
- 網站和LINE機器人數據完全同步
- 即時更新排行榜和用戶統計

## 技術架構

```
網站版本 (web_app.py)
    ↓
LINE Login OAuth 2.0
    ↓
Supabase 數據庫 (與LINE機器人共享)
    ├── users 表 (用戶資料)
    ├── user_stats 表 (遊戲統計)
    └── questions 表 (題目庫)
```

## 部署步驟

### 1. 設置LINE Login Channel

1. 前往 [LINE Developers Console](https://developers.line.biz/)
2. 創建新的Provider（如果還沒有）
3. 在Provider下創建新的Channel
4. 選擇「LINE Login」作為Channel類型
5. 設置以下資訊：
   - Channel name: 解剖學測驗網站
   - App types: Web app
   - Redirect URI: `https://your-domain.com/auth/callback`
   - Scopes: profile, openid

### 2. 獲取LINE Login憑證

從LINE Developers Console獲取：
- Channel ID
- Channel Secret
- 設置正確的Redirect URI

### 3. 環境變數設置

創建 `.env` 文件或設置環境變數：

```bash
# LINE Login 設定
LINE_LOGIN_CHANNEL_ID=your_line_login_channel_id
LINE_LOGIN_CHANNEL_SECRET=your_line_login_channel_secret
LINE_LOGIN_REDIRECT_URI=https://your-domain.com/auth/callback

# LINE Bot 設定 (現有)
LINE_CHANNEL_ACCESS_TOKEN=your_line_bot_access_token
LINE_CHANNEL_SECRET=your_line_bot_channel_secret

# Supabase 設定 (現有)
SUPABASE_URL=https://ciqlfqfgzqqgdrogedxg.supabase.co
SUPABASE_KEY=your_supabase_anon_key

# Flask 設定
FLASK_SECRET_KEY=your_secret_key_here
FLASK_ENV=production

# 部署設定
PORT=5000
```

### 4. 本地測試

```bash
# 安裝依賴
pip install -r requirements.txt

# 設置環境變數
export LINE_LOGIN_CHANNEL_ID="your_channel_id"
export LINE_LOGIN_CHANNEL_SECRET="your_channel_secret"
export LINE_LOGIN_REDIRECT_URI="http://localhost:5000/auth/callback"
export FLASK_SECRET_KEY="your_secret_key"

# 啟動網站
python web_app.py
```

訪問 `http://localhost:5000` 進行測試。

### 5. 部署到雲端平台

#### 選項1: Render.com

1. 連接GitHub倉庫
2. 設置環境變數
3. 設置Build Command: `pip install -r requirements.txt`
4. 設置Start Command: `python web_app.py`

#### 選項2: Heroku

1. 創建 `Procfile`：
```
web: python web_app.py
```

2. 部署：
```bash
heroku create your-app-name
heroku config:set LINE_LOGIN_CHANNEL_ID=your_channel_id
heroku config:set LINE_LOGIN_CHANNEL_SECRET=your_channel_secret
# ... 設置其他環境變數
git push heroku main
```

#### 選項3: Vercel

1. 創建 `vercel.json`：
```json
{
  "version": 2,
  "builds": [
    {
      "src": "web_app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "web_app.py"
    }
  ]
}
```

2. 部署：
```bash
vercel --prod
```

## 使用流程

### 學生使用流程

1. **訪問網站**：學生訪問網站首頁
2. **LINE登入**：點擊「使用LINE登入」按鈕
3. **授權確認**：在LINE中確認授權
4. **自動同步**：系統自動載入現有的遊戲記錄
5. **開始測驗**：開始答題，數據即時同步

### 數據同步機制

- **用戶認證**：使用LINE用戶ID作為唯一識別
- **數據共享**：網站和LINE機器人使用相同的Supabase數據庫
- **即時更新**：答題結果立即更新到數據庫
- **排行榜同步**：網站和LINE機器人的排行榜數據完全一致

## 安全考量

### OAuth 2.0 安全
- 使用state參數防止CSRF攻擊
- 安全的token交換流程
- 自動過期的access token

### 數據保護
- 用戶數據加密存儲
- 安全的session管理
- HTTPS強制使用

## 故障排除

### 常見問題

1. **LINE登入失敗**
   - 檢查Channel ID和Secret是否正確
   - 確認Redirect URI設置正確
   - 檢查網路連接

2. **數據同步問題**
   - 確認Supabase連接正常
   - 檢查用戶ID是否一致
   - 查看伺服器日誌

3. **部署問題**
   - 檢查環境變數設置
   - 確認依賴安裝完整
   - 查看部署平台日誌

### 日誌監控

網站會記錄以下重要事件：
- 用戶登入/登出
- 測驗開始/結束
- 數據庫操作
- 錯誤訊息

## 維護和更新

### 定期維護
- 監控伺服器性能
- 檢查數據庫連接
- 更新依賴套件
- 備份用戶數據

### 功能更新
- 添加新題目到questions表
- 調整等級系統
- 優化用戶界面
- 增加新功能

## 支援和聯絡

如有技術問題或需要協助，請聯絡開發團隊。

---

**注意**：請確保在部署前充分測試所有功能，特別是LINE登入和數據同步功能。
