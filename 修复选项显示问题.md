# 🔧 修复选项显示问题

## 🔍 问题分析

游戏成功加载题目，但选项不显示。可能原因：

1. **数据格式问题** - `options` 字段格式不正确
2. **字段名称不匹配** - 数据库字段名与代码不匹配
3. **数据类型问题** - options 不是 JSON 格式

## 🎯 解决方案

### 步骤 1：检查数据库结构

在 Supabase Dashboard：

1. **进入 Table Editor**
2. **选择 `anatomy_questions_v2` 表**
3. **查看表结构**，确认字段名称：
   - `question` - 题目
   - `options` - 选项（应该是 JSON 格式）
   - `correct_answer` - 正确答案索引
   - `explanation` - 解释
   - `level` - 等级
   - `category` - 类别

### 步骤 2：检查数据格式

查看一条记录的数据，确认 `options` 字段格式：

**正确格式应该是：**
```json
["選項1", "選項2", "選項3", "選項4"]
```

**错误格式可能是：**
- 字符串格式：`"選項1,選項2,選項3,選項4"`
- 其他格式

### 步骤 3：修复数据格式

如果 `options` 字段格式不正确，在 SQL Editor 中执行：

```sql
-- 查看当前数据格式
SELECT id, question, options, correct_answer 
FROM anatomy_questions_v2 
LIMIT 3;

-- 如果 options 是字符串格式，需要转换为 JSON
-- 假设 options 字段存储的是逗号分隔的字符串
UPDATE anatomy_questions_v2 
SET options = '["' || REPLACE(options, ',', '","') || '"]'::jsonb
WHERE options IS NOT NULL;

-- 或者重新插入正确格式的数据
```

### 步骤 4：检查字段名称

如果字段名称不匹配，需要修改代码中的字段名。

## 🔧 临时解决方案

如果无法立即修复数据库，可以修改代码来处理不同的数据格式：

```javascript
// 在 displayQuestion 函数中添加数据格式检查
function displayQuestion(question) {
    console.log('题目数据:', question); // 调试用
    
    // 检查 options 格式
    let options = question.options;
    if (typeof options === 'string') {
        // 如果是字符串，尝试解析
        options = options.split(',').map(opt => opt.trim());
    } else if (Array.isArray(options)) {
        // 如果已经是数组，直接使用
        options = options;
    } else {
        console.error('选项格式错误:', options);
        return;
    }
    
    // 继续显示题目...
}
```

## 📋 需要检查的信息

请告诉我：

1. **`anatomy_questions_v2` 表的字段名称是什么？**
2. **`options` 字段的数据格式是什么样的？**
3. **能否提供一条示例记录的数据？**

这样我可以提供更精确的修复方案！
