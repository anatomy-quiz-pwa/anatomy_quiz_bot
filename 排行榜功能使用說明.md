# 排行榜功能使用說明

## 問題診斷結果

經過詳細調試，發現「輸入排行榜沒有出現對應的 Flex Message」的問題原因是：**環境變數未設置**。

## 功能狀態

✅ **所有功能都正常運作**：
- 文字處理邏輯正確
- 排行榜 Flex Message 創建正常
- 訊息發送函數正常
- 平台檢測（LINE vs Facebook）正常

## 解決方案

### 1. 設置 LINE 環境變數

要讓 LINE 用戶接收排行榜 Flex Message，需要設置以下環境變數：

```bash
export LINE_CHANNEL_ACCESS_TOKEN='your_line_channel_access_token_here'
export LINE_CHANNEL_SECRET='your_line_channel_secret_here'
```

### 2. 設置 Facebook 環境變數

要讓 Facebook 用戶接收訊息，需要設置：

```bash
export PAGE_ACCESS_TOKEN='your_facebook_page_access_token_here'
export VERIFY_TOKEN='your_facebook_verify_token_here'
```

### 3. 獲取 LINE Channel Access Token

1. 登入 [LINE Developers Console](https://developers.line.biz/)
2. 選擇您的 Channel
3. 在「Messaging API」頁面找到「Channel access token」
4. 複製並設置環境變數

### 4. 獲取 Facebook Page Access Token

1. 登入 [Facebook Developers](https://developers.facebook.com/)
2. 選擇您的 App
3. 在「Messaging」頁面找到「Page Access Token」
4. 複製並設置環境變數

## 使用方式

設置環境變數後，用戶可以通過以下方式查看排行榜：

### 文字輸入
- `排行榜`
- `leaderboard`
- `排名`
- `排行`

### 按鈕點擊
- 點擊「查看排行榜」按鈕

## 功能特色

- 🎨 美觀的 Flex Message 格式
- 🏆 前10名排名顯示
- 📊 詳細統計信息（分數、等級、答題數、準確率）
- 📍 個人排名（如果不在前10名）
- 🔄 重新挑戰按鈕
- 💡 鼓勵文字

## 技術細節

### 平台檢測
- LINE 用戶ID（以 `U` 開頭）→ 使用 LINE API
- 其他用戶ID → 使用 Facebook API

### 訊息格式
- LINE: 使用 `push` API 發送 Flex Message
- Facebook: 使用 `send` API 發送訊息

### 錯誤處理
- 環境變數未設置時會記錄錯誤日誌
- 數據庫連接失敗時會發送錯誤訊息
- Flex Message 創建失敗時會回退到文字訊息

## 測試驗證

所有功能都經過測試驗證：
- ✅ 文字處理流程
- ✅ 排行榜訊息創建
- ✅ 訊息發送
- ✅ 環境變數檢測
- ✅ 完整工作流程

## 常見問題

### Q: 為什麼輸入「排行榜」沒有反應？
A: 請檢查環境變數是否正確設置，特別是 `LINE_CHANNEL_ACCESS_TOKEN`。

### Q: 為什麼只收到文字訊息而不是 Flex Message？
A: 可能是 Flex Message 創建失敗，系統會自動回退到文字訊息。

### Q: 為什麼 LINE 用戶收不到訊息？
A: 請確認 `LINE_CHANNEL_ACCESS_TOKEN` 是否正確設置且有效。

### Q: 為什麼 Facebook 用戶收不到訊息？
A: 請確認 `PAGE_ACCESS_TOKEN` 是否正確設置且有效。

## 日誌檢查

查看 `app.log` 文件可以了解詳細的執行情況：
- 用戶訊息接收
- 排行榜數據獲取
- Flex Message 創建
- 訊息發送結果

## 總結

排行榜功能完全正常，只需要正確設置環境變數即可使用。所有代碼邏輯都已經過測試驗證，問題純粹是配置問題。
