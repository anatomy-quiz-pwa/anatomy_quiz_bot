# 🎯 暱稱功能實施完整指南

## 📋 任務概述

**目標**：在 Supabase 的 `user_stats` 表格中添加暱稱欄位，讓排行榜顯示用戶輸入的暱稱、分數、等級以及排行。

## 🔧 實施步驟

### 步驟 1：在 Supabase Dashboard 中添加暱稱欄位

1. **登入 Supabase Dashboard**
   - 前往 [https://supabase.com/dashboard](https://supabase.com/dashboard)
   - 選擇項目：`ciqlfqfgzqqgdrogedxg`

2. **進入 SQL Editor**
   - 左側導航欄 → "SQL Editor"
   - 點擊 "New query"

3. **執行以下 SQL 腳本**：

```sql
-- 1. 添加暱稱欄位
ALTER TABLE user_stats 
ADD COLUMN nickname VARCHAR(50) DEFAULT NULL;

-- 2. 為現有記錄設置默認暱稱
UPDATE user_stats 
SET nickname = CASE 
    WHEN user_id LIKE 'U%' THEN '用戶_' || SUBSTRING(user_id FROM 2 FOR 8)
    WHEN user_id LIKE 'test%' THEN '測試用戶_' || SUBSTRING(user_id FROM 5)
    ELSE '用戶_' || user_id
END
WHERE nickname IS NULL;

-- 3. 查看更新結果
SELECT user_id, nickname, level, correct, wrong, last_update 
FROM user_stats 
ORDER BY correct DESC 
LIMIT 10;
```

### 步驟 2：驗證欄位添加成功

執行以下查詢確認：

```sql
-- 查看表格結構
SELECT column_name, data_type, is_nullable, column_default 
FROM information_schema.columns 
WHERE table_name = 'user_stats' 
ORDER BY ordinal_position;

-- 查看帶暱稱的數據
SELECT user_id, nickname, level, correct, wrong, last_update 
FROM user_stats 
ORDER BY correct DESC;
```

### 步驟 3：運行測試腳本

```bash
python test_nickname_feature.py
```

### 步驟 4：測試應用程序

訪問以下端點確認暱稱顯示：
- http://localhost:5003/leaderboard
- http://localhost:5003/dashboard

## 📊 預期結果

### 表格結構變化
```
新增欄位：
nickname | character varying(50) | YES | NULL
```

### 數據示例
```
user_id                                    | nickname        | level | correct | wrong
------------------------------------------|-----------------|-------|---------|-------
U977c24d1fec3a2bf07035504e1444911         | 用戶_977c24d1   | 8     | 20      | 0
test_comparison                           | 測試用戶_comparison | 1  | 19      | 11
test_correct_qids_fix                     | 測試用戶_correct_qids_fix | 2 | 3 | 0
```

### 排行榜顯示
```
🏆 排行榜（包含暱稱）：
   1. 用戶_977c24d1        | 等級8 | 200分 | 20/20題 | 100.0%
   2. 測試用戶_comparison  | 等級1 | 190分 | 19/30題 |  63.3%
   3. 測試用戶_correct_qids_fix | 等級2 | 30分 |  3/ 3題 | 100.0%
```

## 🔄 代碼修改詳情

### 1. 數據獲取函數更新
```python
def get_real_students_data():
    # 使用暱稱欄位，如果沒有則生成默認名稱
    nickname = record.get('nickname')
    if nickname:
        name = nickname
    else:
        # 如果沒有暱稱，生成默認名稱
        name = generate_default_name(user_id)
```

### 2. 暱稱生成邏輯
```python
def generate_nickname(user_id):
    if user_id.startswith('U') and len(user_id) > 10:
        return f"用戶_{user_id[2:10]}"  # LINE 用戶
    elif user_id.startswith('test'):
        return f"測試用戶_{user_id[5:]}"  # 測試用戶
    else:
        return f"用戶_{user_id}"  # 其他用戶
```

## 🎯 功能特點

### ✅ 實現的功能
1. **暱稱欄位**：在 Supabase 中添加 `nickname` 欄位
2. **自動生成**：為現有記錄自動生成暱稱
3. **排行榜顯示**：排行榜顯示用戶暱稱而非系統生成的用戶名
4. **向後兼容**：如果沒有暱稱，仍使用默認名稱
5. **數據一致性**：排行榜和儀表板使用相同的暱稱數據

### 📈 顯示內容
- **暱稱**：用戶輸入或系統生成的暱稱
- **分數**：基於正確答案數計算的分數（每題10分）
- **等級**：用戶當前等級
- **排行**：按分數降序排列的排名

## 🛠️ 技術實現

### 數據庫修改
- **欄位類型**：VARCHAR(50)
- **允許空值**：是（向後兼容）
- **默認值**：NULL

### 應用程序更新
- **優先使用暱稱**：如果存在暱稱欄位，優先使用
- **回退機制**：如果沒有暱稱，使用默認名稱生成邏輯
- **實時更新**：每次請求都獲取最新的暱稱數據

## 📝 測試清單

### 數據庫測試
- [ ] 暱稱欄位成功添加
- [ ] 現有記錄成功更新暱稱
- [ ] 新記錄可以設置暱稱

### 應用程序測試
- [ ] 排行榜正確顯示暱稱
- [ ] 儀表板正確顯示暱稱
- [ ] 分數計算正確
- [ ] 等級顯示正確
- [ ] 排行順序正確

### 用戶體驗測試
- [ ] 暱稱顯示友好
- [ ] 分數計算合理
- [ ] 排行榜排序正確
- [ ] 頁面載入速度正常

## 🚀 完成標準

**成功標準**：
1. ✅ Supabase 表格包含 `nickname` 欄位
2. ✅ 所有現有記錄都有暱稱
3. ✅ 排行榜顯示暱稱而非系統用戶名
4. ✅ 分數、等級、排行正確顯示
5. ✅ 應用程序正常運行無錯誤

**驗證命令**：
```bash
# 檢查暱稱欄位
python test_nickname_feature.py

# 檢查應用程序
curl http://localhost:5003/leaderboard | grep -E "(用戶_|測試用戶_)"
```

## 📞 需要幫助？

如果在實施過程中遇到問題：
1. 檢查 Supabase Dashboard 權限
2. 確認 SQL 腳本執行成功
3. 查看應用程序日誌
4. 運行測試腳本診斷問題

