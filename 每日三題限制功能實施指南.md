# 每日三題限制功能實施指南

## 📋 功能概述

為解剖學問答機器人添加每日三題限制功能，確保學生每天最多只能回答3個問題，有助於：
- 促進深度學習和消化
- 避免過度刷題
- 培養良好的學習習慣

## 🗄️ 資料庫遷移

### 步驟 1：執行 SQL 遷移

請在 **Supabase 控制台** 的 **SQL Editor** 中執行以下 SQL：

```sql
-- 添加每日答題記錄欄位
ALTER TABLE user_stats 
ADD COLUMN IF NOT EXISTS daily_questions_answered INTEGER DEFAULT 0,
ADD COLUMN IF NOT EXISTS last_question_date DATE DEFAULT CURRENT_DATE;

-- 為現有記錄設置默認值
UPDATE user_stats 
SET 
    daily_questions_answered = 0,
    last_question_date = CURRENT_DATE
WHERE daily_questions_answered IS NULL OR last_question_date IS NULL;

-- 添加註釋
COMMENT ON COLUMN user_stats.daily_questions_answered IS '今日已答題數量，每日重置';
COMMENT ON COLUMN user_stats.last_question_date IS '最後答題日期，用於每日重置';

-- 創建索引以提高查詢效率
CREATE INDEX IF NOT EXISTS idx_user_stats_daily_date ON user_stats(last_question_date);

-- 查看更新結果
SELECT 
    user_id, 
    level, 
    correct, 
    wrong,
    daily_questions_answered,
    last_question_date,
    last_update 
FROM user_stats 
ORDER BY correct DESC 
LIMIT 10;
```

### 步驟 2：驗證欄位添加成功

執行以下查詢確認欄位已添加：

```sql
SELECT 
    column_name, 
    data_type, 
    is_nullable, 
    column_default
FROM information_schema.columns 
WHERE table_name = 'user_stats' 
AND column_name IN ('daily_questions_answered', 'last_question_date')
ORDER BY column_name;
```

## 🔧 程式碼更新

### 已實施的功能：

1. **每日限制檢查函數** (`check_daily_question_limit`)
   - 檢查用戶今天是否還能答題
   - 自動處理每日重置邏輯
   - 返回詳細的限制狀態

2. **每日計數更新函數** (`update_daily_question_count`)
   - 在用戶答題後更新每日計數
   - 記錄最後答題日期

3. **答題流程修改** (`handle_normal_quiz`)
   - 在用戶輸入「開始」時檢查每日限制
   - 顯示達到限制的提醒訊息

4. **統計更新整合** (`update_user_stats_after_answer`)
   - 在更新用戶統計時同步更新每日計數

## 📱 用戶體驗

### 正常答題流程：
1. 用戶輸入「開始」
2. 系統檢查每日限制
3. 如果未達限制，發送題目
4. 用戶回答後，系統更新每日計數

### 達到限制時：
用戶輸入「開始」後收到提醒訊息：

```
⏰ 每日答題次數已達上限

🎯 今日已完成：3/3 題
📚 您已經很努力學習了！

💡 溫馨提醒：
• 每天最多可以答 3 題
• 這樣可以讓您更好地消化學習內容
• 明天 00:00 重置答題次數

🌟 您可以：
• 輸入「排行榜」查看您的排名
• 輸入「積分」查看學習成果
• 明天再來繼續學習新知識

感謝您的堅持學習！🎉
```

## 🧪 測試步驟

### 步驟 1：執行資料庫遷移後，運行測試腳本

```bash
python test_daily_limit_feature.py
```

### 步驟 2：手動測試

1. **測試新用戶**：
   - 使用測試帳號輸入「開始」
   - 確認可以正常答題

2. **測試限制邏輯**：
   - 連續答3題
   - 第4次輸入「開始」時應顯示限制訊息

3. **測試每日重置**：
   - 修改資料庫中的 `last_question_date` 為昨天
   - 輸入「開始」應該可以重新答題

## 🔄 每日重置機制

系統會在以下情況自動重置每日計數：
- 檢查每日限制時發現日期變更
- 用戶首次在新的一天訪問系統

重置邏輯：
```python
if last_question_date < today:
    daily_questions_answered = 0
    last_question_date = today
```

## 📊 管理員例外

管理員用戶不受每日三題限制：
- 管理員可以無限制答題
- 用於測試和內容驗證
- 不影響普通用戶的限制邏輯

## ⚠️ 注意事項

1. **資料庫遷移**：必須先執行 SQL 遷移才能使用功能
2. **時區處理**：系統使用伺服器時區，確保一致性
3. **錯誤處理**：連接失敗時不阻擋用戶答題
4. **效能考量**：添加了索引以提高查詢效率

## 🎯 預期效果

實施後的預期效果：
- ✅ 用戶每天最多答3題
- ✅ 達到限制時顯示友善提醒
- ✅ 每天午夜自動重置
- ✅ 不影響現有功能
- ✅ 管理員不受限制

## 🔧 故障排除

### 常見問題：

1. **欄位不存在錯誤**
   - 確認已執行資料庫遷移
   - 檢查 Supabase 控制台中的表結構

2. **限制不生效**
   - 檢查日誌中的錯誤訊息
   - 確認函數正確調用

3. **重置不正常**
   - 檢查伺服器時區設置
   - 確認日期比較邏輯

如有問題，請檢查應用程式日誌獲取詳細錯誤信息。
